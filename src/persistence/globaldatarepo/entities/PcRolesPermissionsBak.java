package persistence.globaldatarepo.entities;
// default package
// Generated Jun 12, 2017 12:59:07 PM by Hibernate Tools 5.2.0.Beta1

import org.hibernate.Session;
import org.hibernate.SessionFactory;
import persistence.config.PersistenceFactory;
import persistence.enums.HibernateConfigs;

import javax.persistence.*;

import static javax.persistence.GenerationType.IDENTITY;

/**
 * PcRolesPermissionsBak generated by hbm2java
 */
@Entity
@Table(name = "PC_RolesPermissionsBAK", schema = "dbo", catalog = "QAWIZPROGlobalDataRepository")
public class PcRolesPermissionsBak {

	private Integer id;
	private String role;
	private String permission;

	public PcRolesPermissionsBak() {
	}

	public PcRolesPermissionsBak(String role, String permission) {
		this.role = role;
		this.permission = permission;
	}

	@Id
	@GeneratedValue(strategy = IDENTITY)

	@Column(name = "ID", unique = true, nullable = false)
	public Integer getId() {
		return this.id;
	}

	public void setId(Integer id) {
		this.id = id;
	}

	@Column(name = "Role", nullable = false, length = 100)
	public String getRole() {
		return this.role;
	}

	public void setRole(String role) {
		this.role = role;
	}

	@Column(name = "Permission", nullable = false, length = 100)
	public String getPermission() {
		return this.permission;
	}

	public void setPermission(String permission) {
		this.permission = permission;
	}
	
	@Transient
	public static void createNewRolePermission(String role, String permission) throws Exception {
		SessionFactory sessionFactory = null;
		Session session = null;
        PersistenceFactory pf = new PersistenceFactory();
		try {
            sessionFactory = pf.getSessionFactory(HibernateConfigs.GlobalDataRepository);

			session = sessionFactory.openSession();
			session.beginTransaction();

			PcRolesPermissionsBak pcRole = new PcRolesPermissionsBak(role, permission);

			session.save(pcRole);

			session.getTransaction().commit();
		} catch (Exception e) {			
			e.printStackTrace();
			throw new Exception(e.getMessage());
		} finally {
			session.clear();
            pf.close();
		}
	}

}
